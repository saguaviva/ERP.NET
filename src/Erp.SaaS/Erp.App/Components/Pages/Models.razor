@page "/articulos/models"
@attribute [Authorize(Roles = "TenantAdmin,TenantReader")]
@inject AuthenticationStateProvider AuthenticationStateProvider
@inject IModeloQueries ModeloQueries
@inject ILegacySyncService LegacySyncService
@inject NavigationManager NavigationManager

<PageTitle>Artículos / Models</PageTitle>

<div class="page-header">
    <div>
        <p class="eyebrow">ARTÍCULOS</p>
        <h1>Models</h1>
        <p class="lead">@(isLegacySyncActive
            ? "Consulta y filtra los models replicados en la SaaS local. La sync manual refresca solo registros con origen legacy."
            : "Consulta y filtra los models del centro legacy asociado a la empresa activa.")</p>
    </div>
    <div class="quick-links">
        @if (canEdit)
        {
            <a class="primary-button link-button" href="/articulos/models/nuevo">Nuevo model</a>
        }
    </div>
</div>

@if (!tenantId.HasValue || !companyId.HasValue)
{
    <div class="empty-state">
        <p>Selecciona una empresa activa para trabajar con models.</p>
    </div>
}
else
{
    <section class="panel">
        <div class="filter-row">
            <input class="text-input" @bind="filter.Search" placeholder="Buscar por código, descripción, cliente, tejido o código de model" />
            <button class="secondary-button" @onclick="SearchAsync" disabled="@isLoading">@(isLoading ? "Cargando..." : "Buscar")</button>
        </div>
        <div class="form-grid" style="margin-top: 1rem; margin-bottom: 0;">
            <label class="field">
                <span>Temporada</span>
                <InputText class="text-input" @bind-Value="filter.Season" />
            </label>
            <label class="field">
                <span>Serie</span>
                <InputText class="text-input" @bind-Value="filter.Series" />
            </label>
        </div>
        <div class="filter-row results-toolbar" style="margin-top: 0.85rem;">
            <span class="results-summary">
                @(totalCount == 0
                    ? "Sin resultados"
                    : $"Mostrando {PageStart}-{PageEnd} de {totalCount} models")
            </span>
            <label class="field compact-field">
                <span>Tamaño de página</span>
                <select class="sidebar-select compact-select" value="@filter.PageSize" @onchange="OnPageSizeChanged" disabled="@isLoading">
                    <option value="25">25</option>
                    <option value="50">50</option>
                    <option value="100">100</option>
                </select>
            </label>
        </div>
    </section>

    @if (!string.IsNullOrWhiteSpace(errorMessage))
    {
        <div class="alert danger">@errorMessage</div>
    }

    @if (isLegacySyncActive)
    {
        <div class="alert">
            Artículos / Models está en convivencia con legacy para esta empresa.
            La web guarda cambios solo en la SaaS local y la sync manual refresca únicamente registros de origen legacy.
            @if (!string.IsNullOrWhiteSpace(syncStatusLabel))
            {
                <span>@syncStatusLabel</span>
            }
        </div>
    }

    <section class="panel @(isLoading ? "loading-panel" : string.Empty)" aria-busy="@isLoading">
        @if (isLoading)
        {
            <div class="loading-banner">
                <span class="loading-indicator" aria-hidden="true"></span>
                <span>Cargando models...</span>
            </div>
        }

        <div class="table-scroll">
            <table class="data-table">
                <thead>
                    <tr>
                        <SortableHeader Label="Código" IsActive="@(sortColumn == nameof(ModeloListItemDto.Code))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.Code)))" />
                        <SortableHeader Label="Descripción" IsActive="@(sortColumn == nameof(ModeloListItemDto.Description))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.Description)))" />
                        <SortableHeader Label="Temporada" IsActive="@(sortColumn == nameof(ModeloListItemDto.Season))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.Season)))" />
                        <SortableHeader Label="Serie" IsActive="@(sortColumn == nameof(ModeloListItemDto.Series))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.Series)))" />
                        <SortableHeader Label="Cliente" IsActive="@(sortColumn == nameof(ModeloListItemDto.ClientName))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.ClientName)))" />
                        <SortableHeader Label="Tejido" IsActive="@(sortColumn == nameof(ModeloListItemDto.FabricCode))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.FabricCode)))" />
                        <SortableHeader Label="Coste" IsActive="@(sortColumn == nameof(ModeloListItemDto.CostPrice))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.CostPrice), true))" />
                        <SortableHeader Label="Venta final" IsActive="@(sortColumn == nameof(ModeloListItemDto.FinalSalePrice))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.FinalSalePrice), true))" />
                        <th>Detalle</th>
                        <SortableHeader Label="Origen" IsActive="@(sortColumn == nameof(ModeloListItemDto.Origin))" Descending="@sortDescending" OnSort="@(() => SetSortAsync(nameof(ModeloListItemDto.Origin)))" />
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @if (isLoading)
                    {
                        @for (var index = 0; index < 5; index++)
                        {
                            <tr class="table-skeleton-row">
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                                <td><span class="skeleton-line short"></span></td>
                            </tr>
                        }
                    }
                    else if (items.Count == 0)
                    {
                        <tr>
                            <td colspan="11" class="table-empty">No hay models para mostrar.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in items)
                        {
                            <tr>
                                <td><a class="inline-link" href="@BuildEditUrl(item.Id)">@item.Code</a></td>
                                <td>@item.Description</td>
                                <td>@item.Season</td>
                                <td>@item.Series</td>
                                <td>@(string.IsNullOrWhiteSpace(item.ClientName) ? $"Cliente {item.ClientCode}" : item.ClientName)</td>
                                <td>
                                    @if (string.IsNullOrWhiteSpace(item.FabricCode))
                                    {
                                        <span class="muted">-</span>
                                    }
                                    else
                                    {
                                        <a class="inline-link" href="@BuildTejidoUrl(item.FabricCode)">@item.FabricCode</a>
                                    }
                                </td>
                                <td>@item.CostPrice.ToString("0.00##")</td>
                                <td>@item.FinalSalePrice.ToString("0.00##")</td>
                                <td>@($"{item.ScandalloLinesCount} esc. · {item.ColorsCount} col. · {item.ForniturasCount} forn.")</td>
                                <td>@(string.IsNullOrWhiteSpace(item.Origin) ? "local" : item.Origin)</td>
                                <td>
                                    <a class="inline-link" href="@BuildEditUrl(item.Id)">@(canEdit ? "Abrir ficha" : "Ver ficha")</a>
                                    <span class="muted"> · </span>
                                    <a class="inline-link" href="@BuildPrintUrl(item.Id)">Imprimir</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>

        <div class="pagination-bar">
            <span class="muted">Página @filter.Page de @TotalPages</span>
            <div class="pagination-actions">
                <button class="secondary-button" @onclick="() => ChangePageAsync(1)" disabled="@(!CanGoPrevious || isLoading)">Primera</button>
                <button class="secondary-button" @onclick="() => ChangePageAsync(filter.Page - 1)" disabled="@(!CanGoPrevious || isLoading)">Anterior</button>
                <button class="secondary-button" @onclick="() => ChangePageAsync(filter.Page + 1)" disabled="@(!CanGoNext || isLoading)">Siguiente</button>
                <button class="secondary-button" @onclick="() => ChangePageAsync(TotalPages)" disabled="@(!CanGoNext || isLoading)">Última</button>
            </div>
        </div>
    </section>
}

@code {
    [SupplyParameterFromQuery(Name = "q")]
    public string? SearchQuery { get; set; }

    [SupplyParameterFromQuery(Name = "season")]
    public string? SeasonQuery { get; set; }

    [SupplyParameterFromQuery(Name = "series")]
    public string? SeriesQuery { get; set; }

    [SupplyParameterFromQuery(Name = "page")]
    public int? PageQuery { get; set; }

    [SupplyParameterFromQuery(Name = "pageSize")]
    public int? PageSizeQuery { get; set; }

    [SupplyParameterFromQuery(Name = "sort")]
    public string? SortQuery { get; set; }

    [SupplyParameterFromQuery(Name = "desc")]
    public bool? DescQuery { get; set; }

    private Guid? tenantId;
    private Guid? companyId;
    private bool canEdit;
    private bool isLoading;
    private bool isLegacySyncActive;
    private string syncStatusLabel = string.Empty;
    private string errorMessage = string.Empty;
    private string sortColumn = nameof(ModeloListItemDto.Season);
    private bool sortDescending;
    private int totalCount;
    private ModeloFilter filter = new() { Page = 1, PageSize = 25, SortColumn = nameof(ModeloListItemDto.Season) };
    private IReadOnlyCollection<ModeloListItemDto> items = [];

    private int TotalPages => Math.Max(1, (int)Math.Ceiling(totalCount / (double)filter.PageSize));
    private bool CanGoPrevious => filter.Page > 1;
    private bool CanGoNext => filter.Page < TotalPages;
    private int PageStart => totalCount == 0 ? 0 : ((filter.Page - 1) * filter.PageSize) + 1;
    private int PageEnd => totalCount == 0 ? 0 : Math.Min(filter.Page * filter.PageSize, totalCount);

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;
        tenantId = user.GetTenantId();
        companyId = user.GetActiveCompanyId();
        canEdit = user.IsPlatformAdmin() || user.IsInRole(PlatformRoles.TenantAdmin);
        await LoadSyncStatusAsync();
    }

    protected override async Task OnParametersSetAsync()
    {
        ApplyQueryState();
        await LoadAsync();
    }

    private async Task LoadSyncStatusAsync()
    {
        isLegacySyncActive = false;
        syncStatusLabel = string.Empty;
        if (!tenantId.HasValue || !companyId.HasValue)
        {
            return;
        }

        var status = (await LegacySyncService.GetModuleStatusesAsync(tenantId.Value))
            .FirstOrDefault(item =>
                item.CompanyId == companyId.Value &&
                string.Equals(item.ModuleKey, LegacySyncModuleKeys.ArticleModels, StringComparison.OrdinalIgnoreCase));

        if (status is null || !status.HasSuccessfulSync)
        {
            return;
        }

        isLegacySyncActive = true;
        syncStatusLabel = status.LastCompletedUtc.HasValue
            ? $"Última sync: {status.LastCompletedUtc.Value.ToLocalTime():dd/MM/yyyy HH:mm:ss} · Ins: {status.LastInserted} · Act: {status.LastUpdated} · Omi: {status.LastSkipped} · Err: {status.LastErrors}"
            : string.Empty;
    }

    private void ApplyQueryState()
    {
        filter.Search = SearchQuery?.Trim() ?? string.Empty;
        filter.Season = SeasonQuery?.Trim().ToUpperInvariant() ?? string.Empty;
        filter.Series = SeriesQuery?.Trim().ToUpperInvariant() ?? string.Empty;
        filter.Page = Math.Max(PageQuery ?? 1, 1);
        filter.PageSize = PageSizeQuery is 25 or 50 or 100 ? PageSizeQuery.Value : 25;
        sortColumn = string.IsNullOrWhiteSpace(SortQuery) ? nameof(ModeloListItemDto.Season) : SortQuery!;
        sortDescending = DescQuery ?? false;
        filter.SortColumn = sortColumn;
        filter.SortDescending = sortDescending;
    }

    private async Task LoadAsync()
    {
        errorMessage = string.Empty;
        items = [];
        totalCount = 0;

        if (!tenantId.HasValue || !companyId.HasValue)
        {
            return;
        }

        isLoading = true;
        try
        {
            var result = await ModeloQueries.SearchAsync(tenantId.Value, companyId.Value, filter);
            items = result.Items;
            totalCount = result.TotalCount;
        }
        catch (Exception exception)
        {
            errorMessage = exception.Message;
        }
        finally
        {
            isLoading = false;
        }
    }

    private Task SearchAsync()
    {
        filter.Page = 1;
        return ReloadFromCurrentStateAsync();
    }

    private async Task SetSortAsync(string column, bool defaultDescending = false)
    {
        if (sortColumn == column)
        {
            sortDescending = !sortDescending;
        }
        else
        {
            sortColumn = column;
            sortDescending = defaultDescending;
        }

        filter.SortColumn = sortColumn;
        filter.SortDescending = sortDescending;
        filter.Page = 1;
        await ReloadFromCurrentStateAsync();
    }

    private async Task ChangePageAsync(int page)
    {
        filter.Page = Math.Clamp(page, 1, TotalPages);
        await ReloadFromCurrentStateAsync();
    }

    private async Task OnPageSizeChanged(ChangeEventArgs args)
    {
        if (int.TryParse(args.Value?.ToString(), out var pageSize))
        {
            filter.PageSize = Math.Clamp(pageSize, 10, 200);
            filter.Page = 1;
            await ReloadFromCurrentStateAsync();
        }
    }

    private async Task ReloadFromCurrentStateAsync()
    {
        NavigationManager.NavigateTo(BuildCurrentUrl(), replace: true);
        await LoadAsync();
    }

    private string BuildCurrentUrl()
    {
        var parameters = new List<string>();
        AddQuery(parameters, "q", filter.Search);
        AddQuery(parameters, "season", filter.Season);
        AddQuery(parameters, "series", filter.Series);

        if (filter.Page > 1)
        {
            parameters.Add($"page={filter.Page}");
        }

        if (filter.PageSize != 25)
        {
            parameters.Add($"pageSize={filter.PageSize}");
        }

        if (!string.Equals(sortColumn, nameof(ModeloListItemDto.Season), StringComparison.Ordinal))
        {
            parameters.Add($"sort={Uri.EscapeDataString(sortColumn)}");
        }

        if (sortDescending)
        {
            parameters.Add("desc=true");
        }

        return parameters.Count == 0
            ? "/articulos/models"
            : $"/articulos/models?{string.Join("&", parameters)}";
    }

    private static void AddQuery(List<string> parameters, string key, string? value)
    {
        if (!string.IsNullOrWhiteSpace(value))
        {
            parameters.Add($"{key}={Uri.EscapeDataString(value)}");
        }
    }

    private static string BuildEditUrl(Guid id) => $"/articulos/models/editar/{id}";
    private static string BuildPrintUrl(Guid id) => $"/articulos/models/{id}/imprimir";

    private static string BuildTejidoUrl(string fabricCode) =>
        $"/articulos/tejidos/editar/{Uri.EscapeDataString(fabricCode)}";
}
